<%# stuff here is currently all static because we want all the navigations to be the same,
of course for fully dynamic navigations we would have to use the values start_obj and levels that the Widget can have %>

<%= render partial: 'shared/side_nav' %>
